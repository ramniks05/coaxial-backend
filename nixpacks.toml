[phases.setup]
nixPkgs = ['maven', 'jdk17']

[phases.build]
cmds = ['./mvnw clean package -DskipTests']

[phases.start]
cmd = 'java -Dserver.port=$PORT -Dspring.profiles.active=prod -jar target/coaxial-0.0.1-SNAPSHOT.jar'

